import LoginForm from "../components/LoginForm";
import PageWrapper from "../components/shared/PageWrapper";

const Login = () => {
  return <PageWrapper form={<LoginForm />} />;
};

export default Login;
